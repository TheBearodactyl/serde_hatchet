[package]
name = "serde_bsn"
version = "0.1.0"
edition = "2024"
keywords = ["serde", "serialization", "serlang"]
categories = ["encoding"]

[dependencies]
serde = { version = "1.0.228", features = ["derive"] }

[profile.release]
lto = false
codegen-units = 1
opt-level = 3
